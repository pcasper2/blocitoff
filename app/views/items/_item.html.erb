<%= content_tag :li, id: "item-#{item.id}" do %>
  <%= item.name %>
  <%= item.days_left%> days left!
  <%= link_to "delete", [current_user, item], method: :delete, class: "glyphicon glyphicon-ok", remote: true %>
<% end %>